/** Vai trò trong app (suy ra từ group PMH ID). */
export type Role = 'admin' | 'member';

/** Trạng thái đơn đăng ký. */
export type RequestStatus = 'submitted' | 'approved' | 'rejected' | 'delivered' | 'cancelled';
