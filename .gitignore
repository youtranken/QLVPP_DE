node_modules/
dist/
build/
coverage/
*.log
.env
.env.local
# Bí mật production — chỉ có .env.prod.example được commit
deploy/.env.prod
# Credential PMH ID thật dùng để thử trên máy dev
deploy/.env.pmhid
# Chứng chỉ tự ký sinh tại chỗ để thử TLS — khoá riêng KHÔNG bao giờ được commit
deploy/tls/
deploy/backups/
.DS_Store
playwright-report/
test-results/
apps/api/uploads/*
!apps/api/uploads/.gitkeep

# Cấu hình Claude Code cục bộ (theo máy từng người)
.claude/settings.local.json

# Ảnh/log do Playwright MCP sinh khi kiểm thử thủ công
.playwright-mcp/
# Đồ thị tri thức sinh bằng /graphify — tái tạo được, không commit
graphify-out/
